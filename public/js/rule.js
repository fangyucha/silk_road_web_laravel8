//let room_id = window.location.href.split('/').pop()
//document.querySelector('#do_movement').setAttribute('onclick', `window.location.href = '/game/${room_id}'`)
// let userInfo = [];
// let gameData = [];

// Pusher.logToConsole = true;

// Echo.join(`room.${room_id}`)
//     .here((users) => {
//         userInfo = users;
//         // userInfo.forEach(element => {
//         //     element['id'] == Auth:: user() -> id ? my_player_id = element['player_id'] : null;
//         // });
//     })
//     .joining((user) => {
//         //userInfo.push(user);
//     })
//     .listen('.DoMovement', (e) => {
//         console.log('hi')
//     })
